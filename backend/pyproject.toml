[project]
name = "backtest-by-cx-backend"
version = "0.1.0"
description = "FastAPI backend for BackTestByCX"
readme = "README.md"
requires-python = ">=3.12,<3.13"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
